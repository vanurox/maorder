
<div class="row">
            <div class="col-lg-6 col-lg-offset-3 col-md-1 col-md-offset-6 col-sm-2 col-sm-offset-6 col-xs-2 col-xs-offset-3">
  
  <table class="table table-hover table-bordered">
    <thead>
      <tr>
        
        <th>Email</th>
		<th>Edit</th>
		<!-- <th>Delete</th> -->
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
			<tr>
				<td><%= user.email %></td>

				<td><%= link_to("",{:action => 'edit',:id => user.id},:class=>'fa fa-edit fa-2x')%></td></td>

				
			</tr>
			<%end %>

    </tbody>
    <%= paginate @users %>
	<%= page_entries_info @users %>
  </table>

</div>
</div>
	
	
	
	
